{% extends 'dashboard/index.html.twig' %}

{% block title %}Stages{% endblock %}

{% block body %}
<div class="page-wrapper">
    <div class="page-header">
        <h1>Gérez les stages</h1>
        <div class="search-and-button">
            <div class="search-box">
                <span class="search-icon">🔍</span>
                <input type="text" placeholder="Rechercher un stage..." />
            </div>
            <a href="{{ path('app_stage_new') }}" class="btn-main">
                + Ajouter un stage
            </a>
        </div>
    </div>

    <div class="table-card">
        <table class="table-list">
            <thead>
                <tr>
                    <th>ÉTUDIANT</th>
                    <th>ENTREPRISE</th>
                    <th>PROF. SUIVI</th>
                    <th>PROF. VISITE</th>
                    <th>DATES</th>
                    <th>DURÉE</th>
                    <th>ATTESTATION</th>
                    <th>ACTIONS</th>
                </tr>
            </thead>
            <tbody>
            {% for s in stages %}
                <tr>
                    <td>{{ s.etudiant.nom }} {{ s.etudiant.prenom }}</td>
                    <td>{{ s.entreprise.nom }}</td>
                    <td>
                        {% if s.profSuivi %}
                            {{ s.profSuivi.prenom }} {{ s.profSuivi.nom }}
                        {% else %}
                            —
                        {% endif %}
                    </td>
                    <td>
                        {% if s.profVisite %}
                            {{ s.profVisite.prenom }} {{ s.profVisite.nom }}
                        {% else %}
                            —
                        {% endif %}
                    </td>
                    <td>{{ s.dateDebut|date('d/m/Y') }} – {{ s.dateFin|date('d/m/Y') }}</td>
                    <td>{{ s.duree }} semaine</td>
                    <td>
                        <span class="badge-pill">
                            {{ s.statutAttestation }}
                        </span>
                    </td>
                    <td class="actions">
                        <a href="{{ path('app_stage_edit', {'id': s.id}) }}" class="icon-btn edit">✏️</a>
                        {{ include('stage/_delete_form.html.twig', {stage: s}) }}
                    </td>
                </tr>
            {% else %}
                <tr>
                    <td colspan="8">Aucun stage enregistré.</td>
                </tr>
            {% endfor %}
            </tbody>
        </table>
    </div>
</div>
{% endblock %}